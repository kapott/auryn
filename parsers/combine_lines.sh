#!/bin/bash
sort -u


#!/bin/bash
# Expects lines from file A via stdin
# Reads lines from file B via a fixed file
#while read a; do
#  while read b; do
#    echo "$a$b"
#  done < "found_https_paths.txt"
#done